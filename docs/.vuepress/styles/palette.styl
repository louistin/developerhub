.home .hero h1 {
  font-size: 2rem;
}

.home .hero {
  text-align: center;
  font-size: 0.8rem;
  margin-top: 100px;
}

.home .hero .description {
  max-width: 35rem;
  font-size: 1.2rem;
  line-height: 1.3;
  color: #6a8bad;
  margin-top: 200px;
}

.home .hero .description {
  max-width: 35rem;
  font-size: 0.9rem;
  line-height: 1.3;
  color: #6a8bad;
  margin-top: 120px;
}